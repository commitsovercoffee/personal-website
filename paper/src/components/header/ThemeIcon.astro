---
---

<span
    id="themeToggle"
    class="hover:cursor-pointer text-zinc-500 hover:animate-pulse material-icons text-4xl hover:text-zinc-50"
    >dark_mode</span
>

<script>
    const themeToggle = document.getElementById("themeToggle");

    const theme = (() => {
        if (
            typeof localStorage !== "undefined" &&
            localStorage.getItem("theme")
        ) {
            return localStorage.getItem("theme");
        }
        if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
            return "dark";
        }
        return "light";
    })();

    if (theme === "light") {
        document.documentElement.classList.remove("dark");
        themeToggle.innerText = "dark_mode";
    } else {
        document.documentElement.classList.add("dark");
        themeToggle.innerText = "light_mode";
    }

    window.localStorage.setItem("theme", theme);

    const handleToggleClick = () => {
        const element = document.documentElement;
        element.classList.toggle("dark");
        themeToggle.innerText = "light_mode";

        const isDark = element.classList.contains("dark");
        localStorage.setItem("theme", isDark ? "dark" : "light");
        themeToggle.innerText = isDark ? "light_mode" : "dark_mode";
    };

    themeToggle.addEventListener("click", handleToggleClick);
</script>
