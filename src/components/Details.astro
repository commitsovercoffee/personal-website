---
const { summary } = Astro.props;
---

<!--
creates a disclosure widget in which information is visible only when the widget is toggled.
-->
<details class="border-b-2 bg-zinc-900 text-zinc-100 group">
    <summary
        class="p-2 hover:cursor-pointer hover:bg-zinc-800 transition duration-200 ease-in-out group-open:bg-zinc-800 group-open:text-zinc-100"
        >{summary}</summary
    >
    <slot class="px-4 py-4 text-xs text-zinc-900 bg-zinc-400"><slot />
</details>
