---
const { socials } = Astro.props;
---

<!---
contact info for a person or an org.
-->
<address class="px-4 py-2 m-2 rounded-xl bg-zinc-900 w-fit">
    {
        socials.map((social) => (
            <a
                class="text-zinc-400 hover:text-zinc-50 transition duration-300 ease-in-out delay-200 hover:underline decoration-1 p-2"
                href={`https://www.${social.platform}.com/${social.username}`}
            >
                {social.platform}
            </a>
        ))
    }
</address>
