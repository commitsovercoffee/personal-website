---
const { src, alt } = Astro.props;
---

<!--
represents self-contained content, potentially with an optional caption, which is specified using the <figcaption> element.
-->
<figure class="sm border-8 border-gray-800 w-72 bg-white">
    <img class="object-cover border-2 border-white p-4" src={src} alt={alt} />
    <figcaption class="line-clamp-1 text-center p-2 bg-gray-600 text-gray-300">
        {alt}
    </figcaption>
</figure>
