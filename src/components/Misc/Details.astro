---
const { summary } = Astro.props;
---

<!--
creates a disclosure widget in which information is visible only when the widget is toggled.
-->
<details class="border-t-2 bg-gray-600 text-gray-300 group">
    <summary
        class="p-2 hover:bg-gray-300 hover:text-gray-600 transition duration-200 ease-in-out group-open:bg-gray-300 group-open:text-gray-600"
        >{summary}</summary
    >
    <p class="px-6 py-4 mt-6 text-xs"><slot /></p>
</details>
