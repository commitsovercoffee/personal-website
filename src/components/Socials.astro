---
const { platform, username } = Astro.props;
---

<!---
contact info for a person or an org.
-->{
    (
        <a
            class="text-zinc-100 hover:text-zinc-50 transition duration-300 ease-in-out delay-100"
            href={`https://www.${platform}.com/${username}`}
        >
            {platform}
        </a>
    )
}
