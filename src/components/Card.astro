---
const { img, alt, content, title } = Astro.props;
---

<div
    class="card flex-wrap relative w-60 h-80 m-2 rounded-xl overflow-hidden hover:border-sky-500 border-4 duration-300 ease-in"
>
    <img
        src={img}
        alt={alt}
        class="absolute top-0 bottom-0 w-full h-full duration-300 ease-in object-cover"
    />
    <div
        class="card-text w-full overflow-scroll absolute rounded-b bottom-0 left-0 h-1/4 hover:h-full p-4 duration-300 ease-in text-center bg-zinc-900"
    >
        <h2 class="p-2 text-zinc-100">{title}</h2>
        <p class="text-xs text-left">
            {content}
        </p>
    </div>
</div>
